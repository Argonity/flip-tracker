<h1> Login </h1>

<p><%= flash[:error] %></p>
<%# user can be symbol as the user is already created, rather than @user to instantiate a new user %>
<%= form_for(:user) do |f|%>
  <%= f.label :username %>
  <%= f.text_field :username %>

  <%= f.label :password %>
  <%= f.password_field :password %>

  <%= f.submit "Login" %>

<% end %>